import { TranslucentLoadingOverlay } from '@/components/ui/translucent-loading-overlay';

export default function GlobalLoading() {
  return <TranslucentLoadingOverlay fullscreen label="Loading next view..." />;
}
